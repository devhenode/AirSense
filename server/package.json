{
  "name": "airsense-server",
  "version": "1.0.0",
  "description": "Backend server for Airsense - Environmental Monitoring Platform",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "if not exist dist mkdir dist && copy *.js dist && copy .env dist && if not exist dist\\data mkdir dist\\data && if exist data\\*.* copy data\\*.* dist\\data",
    "postbuild": "cd dist && npm install --production",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "seed": "node sample-data.js",
    "deploy": "npm run build && npm run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parser": "^3.2.0",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mongodb": "^6.17.0",
    "multer": "^2.0.1",
    "node-fetch": "^2.6.12",
    "nodemon": "^3.1.10",
    "xlsx": "^0.18.5"
  }
}
